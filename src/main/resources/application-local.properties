# Database configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ping-watch}
spring.datasource.username=${DB_USERNAME:hoofkhanh}
spring.datasource.password=${DB_PASSWORD:hoofkhanh}

# JWT configuration
jwt.secret-key=${JWT_SECRET_KEY:r5NY9xRuGCx1yfZfkGqGT8RVVW3oYncsIaB9VRvIYpU}
jwt.expiration.refresh=${JWT_REFRESH_TOKEN_EXPIRATION:880000}
jwt.expiration.access=${JWT_ACCESS_TOKEN_EXPIRATION:12000000}

# Redis configuration
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.database=${REDIS_DB:0}
spring.data.redis.password=${REDIS_PASSWORD:}

# Mail configuration
spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:hoofkhanh@gmail.com}
spring.mail.password=${MAIL_PASSWORD:hoofkhanh}

# Rate limit configuration
app.rate-limit.enabled=true
app.rate-limit.auth.capacity=10
app.rate-limit.auth.refill-tokens=10
app.rate-limit.auth.refill-seconds=60
app.rate-limit.monitoring.capacity=60
app.rate-limit.monitoring.refill-tokens=60
app.rate-limit.monitoring.refill-seconds=60
